# מערכת לימוד תמרורים — גרסת קובץ יחיד

1. חלץ את קובץ ה-ZIP.
2. פתח את `index.html` בלחיצה כפולה.
3. אין צורך בשרת, באינטרנט או בקבצים נוספים.

לפרסום ב-GitHub Pages: העלה את `index.html` לתיקייה הראשית של המאגר.

המערכת כוללת 394 תמרורים וסמלים. ההתקדמות נשמרת בדפדפן המקומי.
